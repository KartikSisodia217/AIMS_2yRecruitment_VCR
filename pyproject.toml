[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cacr-sp-vcr"
version = "0.1.0"
description = "Visual Commonsense Reasoning with CACR-SP"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
